import React from 'react';
import Header from '../Header/Header';

const Search = () => {
    return (
        <div>
            <Header/>
            <h2>Search</h2>
        </div>
    );
};

export default Search;